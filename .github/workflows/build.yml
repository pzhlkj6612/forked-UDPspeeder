name: Build

on:
  push:
    branches: ["branch_libev"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        method: [make, cmake]
        os:
          - ubuntu-latest
          - ubuntu-24.04-arm
          - windows-latest
          - windows-11-arm
          - macos-26-intel
          - macos-latest
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Windows - Setup MSYS2
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            base-devel
            make
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-make

      - name: Linux - Install Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt update && \
          sudo apt install -y build-essential cmake make

      - name: Build with make
        if: matrix.method == 'make' && runner.os != 'Windows'
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then
            make mac
          else
            make
          fi

      - name: Build with make (Windows)
        if: matrix.method == 'make' && runner.os == 'Windows'
        shell: msys2 {0}
        run: make mingw

      - name: Build with cmake
        if: matrix.method == 'cmake' && runner.os != 'Windows'
        run: |
          cmake -S . -B ../build
          cmake --build ../build

      - name: Build with cmake (Windows)
        if: matrix.method == 'cmake' && runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          cmake -S . -B ../build -G "MinGW Makefiles"
          cmake --build ../build
